name: core_services
description: Core services plugin para microkernel_demo
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.9.0
  flutter: ">=3.24.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

  # Dependencia al core principal
  microkernel_core:
    path: ../../packages/microkernel_core

dev_dependencies:
  flutter_test:
    sdk: flutter