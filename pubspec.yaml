name: my_app
description: "Main Flutter app at repository root"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  #MICROKERNEL ARCHITECTURE:
  microkernel_core:
    path: packages/microkernel_core
  auth_plugin:
    path: plugins/auth
  core_services:
    path: plugins/core_services
